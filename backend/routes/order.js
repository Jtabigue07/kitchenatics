const express = require('express');
const router = express.Router();

// Placeholder - add your order routes here
router.get('/', (req, res) => {
	res.json({ message: 'Orders route - to be implemented' });
});

module.exports = router;

